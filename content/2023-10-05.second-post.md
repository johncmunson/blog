---
title: Interesting, unsolved technical problems for really cool people to solve when they get older in life
---

# Second Post

This is another post.

## Subheading

Lorem ipsum dolor sit amet.

![This is an image](/globe.svg)

```ts
export async function getAllPosts() {
  const allPostsData = await Promise.all(
    filenames.map(async (filename) => {
      const date = filename.split(".")[0];
      const slug = filename.split(".")[1];

      const fullPath = path.join(contentDirectory, filename);

      const file = await read(fullPath);
      matter(file);
      // @ts-expect-error - not a good way to type matter
      const title = file.data.matter.title;

      return {
        slug,
        title,
        date,
      };
    })
  );

  // Sort posts by date
  return allPostsData.sort((a, b) => {
    if (a.date < b.date) {
      return 1;
    } else {
      return -1;
    }
  });
}
```
